variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

services:
  - name: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
    alias: docker

stages:
  - test
  - deploy
  - publish

lint Helm Charts:
  image:
    name: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/alpine/helm:3.20.1@sha256:9ccb6cb780553282592b8628e78c50cd914976d481d0d8fd1f362887194a9c4b
    entrypoint: ["/bin/sh", "-c"]
  stage: test
  script:
    - helm dependency update
    - bash run_tests.sh --lint

test Helm Charts:
  image:
    name: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/alpine/helm:3.20.1@sha256:9ccb6cb780553282592b8628e78c50cd914976d481d0d8fd1f362887194a9c4b
    entrypoint: ["/bin/sh", "-c"]
  stage: test
  script:
    - helm dependency update
    - helm plugin install https://github.com/helm-unittest/helm-unittest --version 1.0.3
    - bash run_tests.sh --unit

integration Tests Helm Charts Mariadb:
  variables:
    ROOTLESS: false
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
  script:
    - |
      apk update && apk add -U curl bash
      sleep 10 # Wait for docker service
      bash run_tests.sh --integration

integration Tests Helm Charts Mariadb no TLS:
  variables:
    ROOTLESS: false
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
  script:
    - |
      apk update && apk add -U curl bash
      sleep 10 # Wait for docker service
      bash run_tests.sh --integration -p http

integration Tests Helm Charts Mariadb Rootless:
  variables:
    ROOTLESS: true
  extends: integration Tests Helm Charts Mariadb

integration Tests Helm Charts Mariadb existing GPG secret:
  variables:
    ROOTLESS: false
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
  script:
    - |
      apk update && apk add -U curl bash gnupg
      sleep 10 # Wait for docker service
      bash run_tests.sh --integration -g existing_secret

integration Tests Helm Charts Mariadb provided GPG key:
  variables:
    ROOTLESS: false
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
  script:
    - |
      apk update && apk add -U curl bash gnupg
      sleep 10 # Wait for docker service
      bash run_tests.sh --integration -g provided

integration Tests Helm Charts Postgresql:
  variables:
    ROOTLESS: false
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/docker:29.3.1-dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
  script:
    - |
      apk update && apk add -U curl bash
      sleep 10 # Wait for docker service
      bash run_tests.sh --integration -d postgresql

integration Tests Helm Charts Postgresql Rootless:
  variables:
    ROOTLESS: true
  extends: integration Tests Helm Charts Postgresql

publish:
  stage: publish
  image: ${CI_DEPENDENCY_PROXY_DIRECT_GROUP_IMAGE_PREFIX}/alpine/helm:3.20.1@sha256:9ccb6cb780553282592b8628e78c50cd914976d481d0d8fd1f362887194a9c4b
  variables:
    REPO_BUCKET: "download.passbolt.com"
    REPO_DIR: "charts"
    CHART_NAME: "passbolt"
  script:
    - |
      export GOOGLE_APPLICATION_CREDENTIALS="$BUCKET_SVC_ACC"
      sh .gitlab-ci/scripts/bin/publishing.sh
  rules:
    - if: "$CI_COMMIT_TAG"
      when: on_success
